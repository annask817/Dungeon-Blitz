import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Masked here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Masked extends EnemyBase
{
    public Masked() {
        setName("masked");
        setHealth(1);
        setDamage(1);
        setSpeed(3.0);
        setCoolTimer(35);
        setImages();
    }
}
